{
  "id": 30,
  "title": "Find High Memory Processes",
  "description": "Display processes consuming the most memory.",
  "filename": "030_find_high_memory_processes.sh",
  "category": "Process Monitoring",
  "script": "#!/bin/bash\nset -euo pipefail\n\nLIMIT=\"${LIMIT:-10}\"\n\nps -eo pid,user,comm,%cpu,%mem --sort=-%mem | head -n \"$((LIMIT + 1))\"\n"
}
